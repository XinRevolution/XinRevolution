@*@model IEnumerable<UserModel>

<div class="mb-5">
    <nav class="navbar navbar-dark bg-secondary">
        <a href="#" class="navbar-brand">使用者管理</a>
        <div class="form-inline">
            <a asp-area="Management" asp-controller="User" asp-action="Create" class="btn btn-primary icon-create" title="新增"></a>
        </div>
    </nav>
</div>

<div class="container-fluid">
    <table class="table table-striped table-bordered table-hover table-sm">
        <thead class="thead-dark">
            <tr>
                <th>#</th>
                <th>帳號</th>
                <th>姓名</th>
                <th>電話</th>
                <th>電郵</th>
                <th>地址</th>
                <th>操作區</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.Account</td>
                    <td>@item.Name</td>
                    <td>@item.Phone</td>
                    <td>@item.EMail</td>
                    <td>@item.Address</td>
                    <td>
                        <a asp-area="Management" asp-controller="User" asp-action="Update" asp-route-id="@item.Id" class="btn btn-secondary icon-update" title="編輯"></a>
                        <a asp-area="Management" asp-controller="User" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-secondary icon-delete" title="刪除"></a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

@section scripts{
    <script>
        initDataTable($('table'));
    </script>
}*@